This directory is reserved for locally generated OpenNeed materials.

Regenerate these files with `python3 scripts/generate_investor_ppt.py`.
The generated outputs are intentionally kept out of git so local exports do not pollute the repo history.
